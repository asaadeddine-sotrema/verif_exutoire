version: '3.8'

services:
  verif_exutoire:
    build: .
    container_name: verif_exutoire_app
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    network_mode: "host"
